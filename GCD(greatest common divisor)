#include<bits/stdc++.h>

using namespace std;
void rotateleft(int b[],int d, int n)
{
d=d%n;
int g_c_d=gcd(d,n);
for(int i=0;i<g_c_d,i++)
{
int j=i;
int k=j+d;
if(k>=n)
{
k=k-n
if(k==i)
{

}
int main()
{
int a,c;
std::cin>>a>>c;

int b[c];
for(int i=0;i<a;i++)
{
int q,w;
std::cin>>q>>w;
for(int i=0 ;i<q;i++)
std::cin>>b[i];

rotate left(b,w,q);
print(b,q);
